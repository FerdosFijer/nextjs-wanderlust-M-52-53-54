import React from 'react';

const DestinationCard = ({destination}) => {
    const {imageUrl, destinationName, country, category, price, duration} =destination;
    return (
        <div>
            
        </div>
    );
};

export default DestinationCard;